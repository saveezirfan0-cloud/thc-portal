/**
 * The §9.12 sender, named on A1 and A2 so the manager knows what to look
 * for. A plain module (not a 'use client' one) so server components get
 * the string, not a client reference.
 */
export const RESET_SENDER = 'admin@thehospitalitycompany.co.uk';
