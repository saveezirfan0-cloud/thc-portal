name: ci
on:
  pull_request:
  push:
    branches: [main]
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Version comes from package.json packageManager; naming it here as well
      # makes the action fail with "multiple versions".
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo lint typecheck test
      - uses: supabase/setup-cli@v1
        with: { version: latest }
      # `supabase start` applies supabase/migrations/* and then supabase/seed.sql.
      - run: supabase start
      # The seed must be re-runnable against an already seeded database
      # (Phase 0). Replaying it here fails the build if an upsert key is
      # missing, and 060_seed_shape.sql below catches any duplicated rows.
      - name: seed is idempotent
        run: psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -v ON_ERROR_STOP=1 -q -f supabase/seed.sql
      # pgTAP RLS suite for admin / client / staff / anon. `supabase test db`
      # runs pg_prove recursively over supabase/tests with --ext .sql/.pg, so
      # supabase/tests/_shared/fixtures.psql is included by the tests and is
      # never picked up as a test of its own.
      - run: supabase test db
      - run: pnpm dlx playwright install --with-deps chromium
      # Build BEFORE the Supabase variables are in scope. `next build`
      # inlines NEXT_PUBLIC_* into the middleware bundle, and the three
      # smoke servers have to stay unconfigured or the Phase 0 smoke tests
      # stop meaning what they say.
      - run: pnpm turbo build
      # The public /apply journey (§2.1) writes to the database, so it
      # needs a configured server. Playwright starts one of its own on
      # 3011 from these two values and blanks them for the other three
      # (see e2e/playwright.config.ts). With them unset the journey skips.
      - name: export the local Supabase stack for the public /apply journey
        run: |
          supabase status -o env \
            --override-name api.url=NEXT_PUBLIC_SUPABASE_URL \
            --override-name auth.anon_key=NEXT_PUBLIC_SUPABASE_ANON_KEY \
            | tr -d '"' >> "$GITHUB_ENV"
      # Playwright directly rather than `turbo e2e:smoke`: the turbo task
      # depends on ^build, and re-entering the build now — cached or not —
      # is exactly what the step above is arranged to avoid.
      - run: pnpm --filter @thc/e2e exec playwright test
