pnpm-lock.yaml
.next
dist
.turbo
docs/scope
docs/pdfs
wireframes
docs
*.md
design-handoff

# Generated by @serwist/next at build time; gitignored, but prettier still
# reads the working tree. docs/14 §6 records the same treatment for the
# design-handoff vendor bundles, and for the same reason: `pnpm format`
# rewriting build output is noise in somebody's diff.
apps/staff/public/sw.js
apps/staff/public/sw.js.map
apps/staff/public/swe-worker-*.js
