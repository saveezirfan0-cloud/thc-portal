# Full-page captures and the harness's rendered HTML are regenerated by
# design-pass/harness (run.mjs → shoot.mjs / peek.mjs / pair.mjs); only the
# curated before/after pairs in key/ are kept.
before/
after/
peek/
harness/out/
